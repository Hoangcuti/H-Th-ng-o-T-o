@model COTHUYPRO.Models.LessonFormVm
@{
    ViewData["Title"] = "Chỉnh sửa bài giảng";
    var courses = ViewBag.Courses as List<COTHUYPRO.Models.Course> ?? new();
}

<div class="d-flex justify-content-between align-items-center mb-4 animate-fade-in">
    <div>
        <h4 class="fw-bold m-0 text-dark">Hiệu chỉnh nội dung</h4>
        <p class="text-muted small m-0">Cập nhật thông tin bài giảng: <span class="text-primary fw-bold">@Model.Title</span></p>
    </div>
    <a href="javascript:history.back()" class="btn btn-outline-secondary rounded-pill px-4 bg-white shadow-sm">
        <i class="bi bi-arrow-left me-1"></i> Quay lại
    </a>
</div>

<form asp-action="EditLesson" method="post" enctype="multipart/form-data" class="animate-fade-in delay-100">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="Id" />
    <div class="row g-4">
        <!-- LEFT COL: CONTENT -->
        <div class="col-lg-8">
            <div class="card border-0 shadow-sm rounded-4 bg-white mb-4">
                <div class="card-body p-4">
                    <div class="mb-4">
                        <label class="form-label fw-bold text-dark">Tiêu đề bài học</label>
                        <input asp-for="Title" class="form-control form-control-lg border-2 shadow-none" 
                               placeholder="Ví dụ: Giới thiệu về ASP.NET Core" style="border-color: #f1f5f9; font-weight: 500;" />
                        <span class="text-danger small" asp-validation-for="Title"></span>
                    </div>

                    <div class="mb-0">
                        <div class="d-flex justify-content-between align-items-center mb-2">
                            <label class="form-label fw-bold text-dark m-0">Nội dung chi tiết</label>
                            <button type="button" class="btn btn-sm btn-outline-info rounded-pill px-3 fw-bold" onclick="generateAiLesson(event)">
                                <i class="bi bi-stars me-1 text-primary"></i> Trợ lý AI sửa đổi nội dung
                            </button>
                        </div>
                        <textarea asp-for="Content" class="form-control border-2 shadow-none" rows="12" 
                                  placeholder="Nhập nội dung bài giảng tại đây (hỗ trợ HTML)..." style="border-color: #f1f5f9;"></textarea>
                        <span class="text-danger small" asp-validation-for="Content"></span>
                    </div>
                </div>
            </div>

            <div class="card border-0 shadow-sm rounded-4 bg-white">
                <div class="card-header bg-white border-0 pt-4 px-4 pb-0">
                    <h6 class="fw-bold m-0 text-dark">Học liệu & Video hiện tại</h6>
                </div>
                <div class="card-body p-4">
                    <div class="row g-3">
                        <div class="col-md-12">
                            <label class="form-label small fw-bold text-muted text-uppercase">Link Video (YouTube/Vimeo)</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-0"><i class="bi bi-play-circle-fill text-danger"></i></span>
                                <input asp-for="VideoUrl" class="form-control border-0 bg-light" placeholder="https://www.youtube.com/watch?v=..." oninput="updateVideoPreview(this.value)" />
                            </div>
                            <span class="text-danger small" asp-validation-for="VideoUrl"></span>
                            
                            <div id="videoPreviewContainer" class="mt-3" style="display:none;">
                                <div class="ratio ratio-16x9 border-0 rounded-4 overflow-hidden shadow-sm">
                                    <iframe id="videoPreviewFrame" src="" allowfullscreen></iframe>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-12 mt-4">
                            <label class="form-label small fw-bold text-muted text-uppercase">Tài liệu học tập (Thay đổi file mới nếu cần)</label>
                            <div class="p-4 rounded-4 border-2 border-dashed bg-light text-center position-relative hover-bg-light-blue transition-all">
                                <i class="bi bi-file-earmark-arrow-up fs-2 text-primary"></i>
                                @if(!string.IsNullOrEmpty(Model.DocumentUrl)) {
                                    <div class="mt-2 text-primary fw-bold">Hiện tại: @Model.DocumentUrl.Split('/').Last()</div>
                                    <p class="text-muted small">Bấm vào đây hoặc kéo thả file để thay thế tài liệu hiện tại.</p>
                                } else {
                                    <div class="mt-2 text-dark fw-bold">Kéo thả hoặc chọn file mới</div>
                                    <p class="text-muted small">Tối đa 20MB. .pdf, .docx, .zip</p>
                                }
                                <input type="file" asp-for="DocumentFile" class="position-absolute top-0 start-0 w-100 h-100 opacity-0 cursor-pointer" accept=".pdf,.doc,.docx,.zip" />
                                <input asp-for="DocumentUrl" type="hidden" />
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- RIGHT COL: SETTINGS -->
        <div class="col-lg-4">
            <div class="card border-0 shadow-sm rounded-4 bg-white mb-4">
                <div class="card-header bg-white border-0 pt-4 px-4 pb-0">
                    <h6 class="fw-bold m-0 text-dark">Thông tin bổ trợ</h6>
                </div>
                <div class="card-body p-4">
                    <div class="mb-3">
                        <label class="form-label small fw-bold text-muted text-uppercase">Khóa học</label>
                        <select asp-for="CourseId" class="form-select border-0 bg-light rounded-3 shadow-none">
                            @foreach (var c in courses) { 
                                <option value="@c.Id" selected="@(Model.CourseId == c.Id)">@c.CourseName</option> 
                            }
                        </select>
                    </div>

                    <div class="mb-3">
                        <label class="form-label small fw-bold text-muted text-uppercase">Thứ tự ưu tiên</label>
                        <input asp-for="OrderIndex" class="form-control border-0 bg-light rounded-3 shadow-none" type="number" />
                        <span class="text-danger small" asp-validation-for="OrderIndex"></span>
                    </div>

                    <div class="mb-3">
                        <label class="form-label small fw-bold text-muted text-uppercase">Thời lượng (Phút)</label>
                        <input asp-for="Duration" class="form-control border-0 bg-light rounded-3 shadow-none" type="number" />
                        <span class="text-danger small" asp-validation-for="Duration"></span>
                    </div>

                    <hr class="my-4 opacity-5" />

                    <div class="form-check form-switch mb-3 text-success">
                        <input class="form-check-input shadow-none" type="checkbox" asp-for="IsFreePreview">
                        <label class="form-check-label fw-bold">Cho phép học thử</label>
                    </div>

                    <div class="form-check form-switch mb-4">
                        <input class="form-check-input shadow-none" type="checkbox" asp-for="IsPublished">
                        <label class="form-check-label fw-bold text-dark">Đang xuất bản</label>
                    </div>

                    <div class="p-3 rounded-4 border-0 small text-muted" style="background-color: #f8fafc;">
                        <i class="bi bi-info-circle me-1"></i> Sau khi lưu, nếu bạn là Giảng viên, trạng thái sẽ chuyển về **Chờ duyệt** để đảm bảo chất lượng nội dung.
                    </div>
                </div>
            </div>

            <div class="card border-0 shadow-sm rounded-4 bg-white p-3">
                <button class="btn btn-primary btn-lg w-100 rounded-pill fw-bold shadow-sm py-3 mb-2" type="submit">
                    Lưu các thay đổi
                </button>
                <a class="btn btn-link text-muted text-decoration-none w-100 small" asp-action="Index">Hủy và thoát</a>
            </div>
        </div>
    </div>
</form>

<style>
    .cursor-pointer { cursor: pointer; }
    .hover-bg-light-blue:hover { background-color: rgba(14, 165, 233, 0.05) !important; border-color: var(--primary) !important; }
    .form-control:focus, .form-select:focus { background-color: #fff; box-shadow: 0 0 0 4px rgba(14, 165, 233, 0.1); border-color: var(--primary); }
    .btn-outline-info { border-color: #e2e8f0; color: #64748b; }
    .btn-outline-info:hover { background-color: #f1f5f9; border-color: #cbd5e1; color: #334155; }
</style>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
    async function generateAiLesson(e) {
        var topic = prompt("Nhập chủ đề hoặc yêu cầu chỉnh sửa cho AI:");
        if(!topic) return;
        
        var btn = e.target;
        var originalText = btn.innerHTML;
        btn.innerHTML = "<span class='spinner-border spinner-border-sm me-2'></span>Đang xử lý...";
        btn.disabled = true;
        
        try {
            var res = await fetch('/Instructor/GenerateLessonContent', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ topic: `Chỉnh sửa bài giảng chủ đề: ${topic}. Nội dung hiện tại: ${document.getElementById('Content').value}` })
            });
            var data = await res.json();
            if(data.success) {
                document.getElementById('Content').value = data.content;
                alert("✨ AI đã cập nhật nội dung! Hãy kiểm tra lại nhé.");
            }
        } catch(err) {
            alert("Lỗi kết nối AI.");
        } finally {
            btn.innerHTML = originalText;
            btn.disabled = false;
        }
    }

    function updateVideoPreview(url) {
        const container = document.getElementById('videoPreviewContainer');
        const iframe = document.getElementById('videoPreviewFrame');
        if (!url) { container.style.display = 'none'; return; }

        let embedUrl = "";
        if (url.includes("youtube.com/watch?v=")) {
            const id = url.split("v=")[1].split("&")[0];
            embedUrl = `https://www.youtube.com/embed/${id}`;
        } else if (url.includes("youtu.be/")) {
            const id = url.split("youtu.be/")[1].split("?")[0];
            embedUrl = `https://www.youtube.com/embed/${id}`;
        } else if (url.includes("vimeo.com/")) {
            const id = url.split("vimeo.com/")[1].split("?")[0];
            embedUrl = `https://player.vimeo.com/video/${id}`;
        }

        if (embedUrl) {
            iframe.src = embedUrl;
            container.style.display = 'block';
        } else { container.style.display = 'none'; }
    }

    window.addEventListener('DOMContentLoaded', () => {
        const val = document.getElementById('VideoUrl').value;
        if (val) updateVideoPreview(val);
    });
    </script>
}
